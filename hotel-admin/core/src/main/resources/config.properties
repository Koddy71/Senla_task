room.status.change.enable=true
room.history.limit=5
storage.type=jpa

db.url=${DB_URL}
db.user=${DB_USER}
db.password=${DB_PASSWORD}
db.driver=${DB_DRIVER}

jwt.secret=${JWT_SECRET:hotel-admin-demo-secret-key-32-bytes-minimum}
jwt.expiration-ms=${JWT_EXPIRATION_MS:43200000}